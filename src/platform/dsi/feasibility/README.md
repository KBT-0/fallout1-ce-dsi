# Retired Phase 0A shim location

The PROJECT_v3 Phase 0A link shim was promoted into the production backend.
The engine-facing SDL-compatible API and implementation now live in:

```text
src/platform/dsi/runtime/include/SDL.h
src/platform/dsi/runtime/dsi_sdl.cc
```

This directory remains only to document where the accepted Phase 0A files went.
